#!/usr/bin/env bash
# install.sh — PAF installer stub
#
# Intended interface:
#   ./scripts/install.sh [--update] [--tool claude|cursor] [--target <dir>]
#
# What this script will do when implemented:
#   1. Detect or accept the target tool (default: Claude Code)
#   2. Copy skills/  → ~/.claude/skills/paf/<skill-name>/
#   3. Copy agents/  → ~/.claude/agents/paf/
#   4. Copy hooks/   → ~/.claude/hooks/paf/ (wire into ~/.claude/settings.json)
#   5. Print a post-install summary listing installed skills and their invoke commands
#
# Not yet implemented. Run install steps manually per the README.

echo "Installer not yet implemented. Run install steps manually per the README."
exit 1
